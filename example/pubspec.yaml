name: example
description: Example for flutter_translate_codegen_annotations
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter_translate_codegen_annotations:
    path: ../
